"use strict";
require = global.require;
var utils = require("@zoho/lyte-cli-utils"),
	fileManipulation = utils.fileManipulation,
	folders =utils.commonCliUtils,
	path = require("path"),
	filePath;
module.exports ={
	description :"services",
	fileMapTokens :function(options)	{
		return {
			__demo__ : options.cliArgs[2]
		};
	},
	
	locals :function(options)  {
		return {
			id : options.cliArgs[2]
		};
	},

	beforeInstall : function(options) {
		if(!folders.checkForAlreadyExistence(options,'service')) {
			if(options.d == '.') {
				options.d = undefined;
			}
		} else {
			this.reject(new Error("service named "+options.cliArgs[2]+" already registered. Service with same name already exists in the src folder of the services"))
		}		
	},

	beforeUninstall : function(options){
		var subFolder;
		subFolder = (options.d && options.d != '.') ? options.d :'';
		filePath=path.join(options.folders.src.services,subFolder,options.cliArgs[2]+'.js')	
		if(!fileManipulation.fileExist(filePath)){	
			this.reject(new Error("Service named "+filePath+" doesn't exists"));	
		}
	},

	uninstall : function(options) {	
		var permitFlag = true;
	   	fileManipulation.removeSync(filePath,permitFlag);	
		options.log.user({color : 'green', msg : "Removed File : "+path.relative(options.root,filePath)});
		if(fileManipulation.fileExist(options.logPath)) {
			options.log.system("Removed File : "+path.relative(options.root,filePath)+'\n');
		}
		folders.removeEmptyFolder(filePath,"services",options);
	}
};
