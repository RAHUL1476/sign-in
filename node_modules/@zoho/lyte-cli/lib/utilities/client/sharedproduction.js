
self.addEventListener("connect", function(e) { 
    var port = e.ports[0];
    port.addEventListener("message", function(e) {   
        port.postMessage({msg : JSON.stringify(response)});
    }, false);
    port.start(); 
}, false); 